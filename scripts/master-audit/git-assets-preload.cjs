const fs=require('fs'),path=require('path'),cp=require('child_process');const exists=fs.existsSync;const files=new Set(cp.execFileSync('git',['ls-tree','-r','--name-only','HEAD'],{encoding:'utf8'}).trim().split('\n'));fs.existsSync=(p)=>{if(exists(p))return true;try {const abs=p instanceof URL?require('url').fileURLToPath(p):String(p);const rel=path.relative(process.cwd(),abs);return rel.startsWith('public/')&&files.has(rel);}catch{return false;}};
