const fs=require('fs'),crypto=require('crypto'),{root}=require('./runtime.cjs');const lock=JSON.parse(fs.readFileSync(root+'/docs/verification/master-audit-20260925/source-lock.json'));const changes=[];for(const [p,sha256]of Object.entries(lock.files)){let actual=null;try{actual=crypto.createHash('sha256').update(fs.readFileSync(root+'/'+p)).digest('hex')}catch{}if(actual!==sha256)changes.push({path:p,expected:sha256,actual});}console.log(JSON.stringify({status:changes.length?'REVIEW_REQUIRED':'MATCH',baseSha:lock.baseSha,changes},null,2));process.exitCode=changes.length?1:0;
