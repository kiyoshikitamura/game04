import fs from 'node:fs';import{simulateBalanceBattle}from './battleBalanceV2.ts';import{rules}from './model.mjs';const src=JSON.parse(fs.readFileSync('battle-check/holdout14-results.json')).results;const results=[];for(const mode of ['combo','dot_only','heavy_without_setup'])for(const variant of ['original_rear','no_rear_attack']){const b=src.find(x=>x.id==='10-6'&&x.phase==='first'&&x.mode===mode),party=structuredClone(b.party),waves=structuredClone(b.waves);waves.at(-1)[1].stats.hp=45000;if(variant==='no_rear_attack')party[1].skills=[];const runs=[];for(let seed=9301;seed<=9400;seed++){const r=simulateBalanceBattle({rules,seed,party,waves}),l=r.frames.at(-1);runs.push({seed,win:r.outcome==='win',actions:r.playerActions,hp:l.party.reduce((s,p)=>s+p.hp,0)/l.party.reduce((s,p)=>s+p.maxHp,0)});}results.push({mode,variant,party,waves,runs});}fs.writeFileSync('battle-check/rear14-results.json',JSON.stringify({status:'CANDIDATE_NOT_FIXED',results}));
