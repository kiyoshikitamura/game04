# GAME04 素材・Master・登用・商店の先行反映（2026-09-18）

## Authority

- GAME04基準：`codex/game04-upstream-20260918`
- GAME03同期基準：`bc2f256eddfaa0b53ba136dd7e84f1da21286ed2`
- 名称：mainの `GAME04_SENGOKU_ITEM_SKILL_EQUIPMENT_NAME_FIX_2026-09-16.md` と参照先6文書をそのまま復元。
- 画像：Repository格納済のItem18・汎用Skill45点（`aa72725`まで）。画像の追加生成・補作なし。

## 対象と到達点

| 項番 | 変更・到達点 | 残課題 |
|---|---|---|
| #18 Skill | 承認済70名称を一覧・カード・詳細・登用結果・提供割合へ共通反映。既存45画像接続を確認 | 汎用5点と専用20点の戦国画像 |
| #19 Equipment | 承認済170名称と専用武将表示を共通反映。全画像pathの実在を確認 | 汎用160点と専用10点の戦国画像 |
| #23 画像配信 | 258件のID・日本語名・実path・実形式・寸法・戦国画像反映状態をmanifest化。既存参照pathを維持 | 素材待ち195点の将来差替え |
| #31 カード表示 | canonical表示MasterからCharacter60・Skill70・Equipment170の戦国名を解決。既存戦国画像を共通カードへ使用 | 未提供のカード画像・枠等の完成判定は後工程 |
| #69 Gacha Pool | 排出IDはGAME03の実Poolを保持しGAME04表示Masterに接続。専用条件・SSR交換条件も不変 | 専用要素の再命名が必要な場合は別途FIX |
| #72 提供割合 | Normalは実Poolのrarity件数とDB提供率から各対象の通常／無料率を表示。SpecialはRPCの個別率をそのまま戦国名で表示 | 公開前の最終実機確認 |
| #76 商品表現 | 商品タイトル・説明・Item18名称・召喚札・ダイヤ表示を共通化。価格・数量・回数制限・期限・税表示は維持 | 商品画像の差替え |

#18・#19・#31・#76は画像待ちを含むため、項目全体の完了とはしない。#23は現素材の配信整理、#69・#72はPreview反映確認後に反映済へ進められる。

## 実装

- `src/theme/sengoku-masters.json`：名称258件。日本語名だけを持ち、数値データを複製しない。
- `src/theme/masters.ts`：名称とItem説明の表示変換。
- `canonical/masters.ts`、`canonical/items.ts`：数値原本JSONを維持し、表示名のみ投影。
- `normalGachaRates.ts`：Normal RPCと同じ「レアリティ抽選 → 同レアリティ内均等抽選」を表示へ反映。
- `specialGacha.ts`：既存RPC検証後、item_idによる表示名解決。提供率・天井・Pt・排出条件は変更なし。
- `shop_master_data.ts`：既存商品定義を保持し、最後に名称・説明を投影。
- `config/game04-master-assets.json`：実在する258画像の配信一覧。英語由来のファイル名は互換性のため維持し、未使用とみなして削除しない。

## 最小検証

- `tsc --noEmit`：PASS（2026-09-18 ローカル）。
- 承認済名称258件とSkill70／Equipment170のID網羅：PASS。
- Skill70／Equipment170の名称以外の全フィールドが変更前と一致：PASS。
- 商品ID・価格・数量・回数制限・期限を変更前と比較：完全一致。
- Normalの例で個別確率合計100%・Specialの名称変換後確率維持：PASS。
- Item18／Skill70／Equipment170の全258pathが実在・画像デコード可能：PASS。
- live DBのNormal抽選関数が同rarity内 `ORDER BY random()` であることを親担当が確認。
- build・Preview URL・配信SHAは統合担当の配信記録に記載。

## DB反映

同JSONをAuthorityに、GAME04 devの `canonical_skill_master`／`canonical_equipment_master`／`canonical_item_master` の `display_name` だけを更新済み。Battle snapshot・RPC経由の表示も同じ名称に揃える。GAME03 DB、排出テーブル、数値・関数ロジックは変更しない。
