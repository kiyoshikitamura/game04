# GAME04 第17回 承認済み62面：反映前照合

状態：**設計承認済み／開発環境への実装反映は未完了**。
本記録は設計の再承認を求めるものではない。承認状態は `round17_approval.md` に従う。

## 作業基準

- Repository: `kiyoshikitamura/game04`
- 取得・再確認した基準ブランチ: `codex/game04-upstream-20260918`
- 最新SHA: `60fe2f382c56c4902ca5eecaf38401a491d02c0e`
- 数値資料SHA: `f8741827daf9ffbb76b097c6d12a3950434af449`
- 専用ローカルブランチ: `codex/game04-round17-acceptance-20260922`
- 読み取り対象DB: `lrgyllgzcdcphlbmkknc` のみ。
- 既存の共有チェックアウトを変更せず、独立したsparse checkoutで確認。`sources/` は未変更。

## 実装反映を阻む既存状態

1. `src/domain/redesign/quests.ts` は `STAGE_NAMES` 7件を10エリアへ展開する仮の70面構成。正本の65面構成ではない。敵は配置ごとにキャラを循環選択し、仮の成長式から生成する。
2. 現行API `supabase/functions/game04-redesign-api/source.ts` の新規通常戦闘は `getQuestStage()` → `stage.waves` → `prepareBattleWaves()` で入力を生成する。通常クエスト敵をDBの `release_manifest` から読み込んではいない。DB値だけを更新しても実戦入力は切り替わらない。
3. 開発DB `game04_redesign_master` の `release_manifest` も10エリア各7面、計70面188Wave404体。`status=PREVIEW_PROVISIONAL`、更新日時 `2026-09-19 14:42:47.853979+00`。対象外の10-8〜10-10はこのmanifestに存在しない。
4. 配信済み `game04-redesign-api` v11を読み取り、同じ7面定義と `waves = stage.waves` の経路を確認。ローカルだけの古い状態ではない。
5. 承認対象のうち7-8、8-8、9-8、9-9、9-10の5面が現行経路に存在しない。現在のステージ構成から65面へ入れ替えると、`QUEST_STAGES` の順序を使う解放条件や、別管理されていない報酬・消費体力との接続にも作業が及ぶ。敵差分だけの更新として実施できない。
6. 現行の `prepareBattleWaves()` は敵スキルを `commonPreviewSkill()` に通し、パッシブのフィルタも行う。正式入力を保存できたことだけでは、実際に戦闘へ渡る値の一致を保証しない。
7. 味方の正式値・正式スキル接続も今回の検証入力とは独立。今回の検証パーティを所持品・配布・自動編成へ転用していない。

必要な連携情報：並走中の65面マスター・正式値接続実装のブランチまたはPR。なければ、65面構成への接続を別工程として扱う範囲の調整が必要。既存の70面仮生成値を「監査開始時before」と見なして上書きしない。

## 完了した再現検証

実行コマンド（リポジトリルート、Node v22.16.0）:

```sh
node --experimental-strip-types scripts/verify_game04_round17_acceptance.mjs docs/development/GAME04_ROUND17_PREINTEGRATION_20260922.json
```

- 計算器3ファイルは保存SHA256と一致。WindowsのCRLFのみLFへ正規化して比較。
- 承認入力62面336体のステージ・Wave・配置位置・敵IDを照合。
- スキル照合記録のキャラIDを正式キャラ表・現行素材ロスターのIDへ照合し、表示名も一致。名前だけの紐付けはしない。
- スキル参照284件のID・優先順、照合記録にあるLB候補と正式792行表のSPを確認。これをスキル効果意味論の新規検証済みとは扱わない。
- 今回39面150体、累計49面175体を確認。累計の各candidate値と確定入力が一致。
- 全62面で既存 `createFormalBattleInput()` が敵入力を欠落なく複製することを確認。ただし現行APIはこの関数で正式62面を生成しておらず、**実戦経路の一致ではない**。
- 最終40面すべてで主軸・対策除去・標準攻撃置換・実キャラ代替・周回の各条件から保存された先頭・中央・末尾seedを再実行（600戦）。本体 `simulateBattle()` を経由し、勝敗、行動数、残HP比、生存数、行動不能スキップ、味方／敵スキル回数、記録対象効果回数が保存結果と全件一致。
- 7-7、8-3、9-9、10-7、3-1、3-2、6-2、8-2も上記に含む。探索途中の別案は使用していない。

詳細の各stage/mode/seedは同梱JSONへ保存。

## 共有参照先・対象外

- 現行通常戦闘とエンカウントは `prepareBattleWaves()` を共有する。将来の通常敵修正を共通変換へ無条件に追加するとレイドへ波及するため、正式通常入力の境界を分ける必要がある。
- エンカウントは `raidEnemy()` と専用Master、侵攻は `territorySnapshot` の敵・戦闘ルールを使用する。調査した現行ドメイン/API経路では通常クエスト `stage.waves` の直接流用なし。独自設定・既存開催snapshot未変更。
- `QUEST_STAGES` はクエスト一覧・Homeの進行表示・ミッション対象検証・解放条件にも使われる。65面接続の統合時に確認が必要。
- GAME03、Production、共通戦闘ルール、味方共通値、経済、報酬、消費体力、解放条件、UI・演出・素材ファイルは変更していない。
- 対象外10-8〜10-10の承認資料は未変更。現行実装に存在しないため「実戦で維持確認済み」とはしない。

## 未完了と変更実績

- 実装反映済みステージ・敵件数：0。実装SHAなし。
- DB書き込み・Edge配信・Preview配信：なし。固定URL・配信SHAの新規発行なし。
- 全62面の実戦入力一致：未完了。
- 実再生時間（倍速・カットイン込み）と保護解除／状態解除／割込みの視覚認知：未測定。行動数を秒数へ換算していない。
- 変更ファイルは本記録、再実行スクリプト、結果JSONのみ。
- 統合先判明後、最新ブランチを再取得して現行敵のbefore／反映済み／並走差分を分類し、正式スキル定義を維持した限定反映、実戦入力検証、画面受入を続行する。
