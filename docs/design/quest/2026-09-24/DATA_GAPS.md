# 出陣 正式データの表示上の不足（2026-09-24）

## 調査対象

- `src/domain/redesign/data/quest65.json`、`questMaster.ts`、`quests.ts`。
- 正式構成は10エリア・65ステージ、最大6Wave。数値・解放・報酬・既存進行は変更しない。

## ステージ名・攻略ヒント

正式Masterのステージ名は攻略意図の文章で、独立した物語上のステージ名は収録されていない。65件中65件で `name` と `description` が同じ文章。

| 正式番号 | 正式name | 正式description |
| --- | --- | --- |
| 1-1 | 初期3人で戦闘を学ぶ | 初期3人で戦闘を学ぶ |
| 1-2 | 受ける役を決める | 受ける役を決める |
| 1-3 | 一つ変えて突破する | 一つ変えて突破する |
| 2-1 | まず1体減らす | まず1体減らす |

UIは正式nameとdescriptionをそのまま接続する。添付モックの「街道の先へ」等への置換、独自の攻略助言や物語名の生成は行わない。正式タイトル・独立した攻略ヒントを新設する場合のみ、商品仕様として別途確定が必要。現在の表示実装は進行可能。

## SP・編成

共通SPは個別値合算ではなく上限400、戦闘開始0。既存正式 `validateDeck` は5人を要求する。モック数値や「初期3人」というステージ名を根拠に編成条件を変更しない。

## 素材の履歴上の不足

前回の `docs/design/quest/2026-09-22/QUEST_COMPLETE_FINISH_VERIFICATION_2026-09-23.md` に明智光秀のバトル素材不足がある。これは過去記録であり、今回の素材担当による現物確認結果を優先する。代替のGAME03素材・無関係画像で補完しない。

## 実API検証

今回専用 `scripts/verify_game04_quest65_live_20260924.mjs` は lrgyllgzcdcphlbmkknc に限定し、新規QAアカウントだけで実行する。既存ユーザーの読込・進行リセットは行わない。実行結果は `LIVE_API_VERIFICATION.json` に記録し、静的監査・ドメイン検証と区別する。DB/API配信は実施しない。

実行結果：2026-09-23T17:45:33Z、新規QA登録リクエストが10秒でtimeout。アカウント作成は応答未取得のため未確認、初期化・戦闘APIには未到達。再送・追加消費・再読込の実API検証は未実施。ネットワーク回復後、専用scriptを再実行する。
