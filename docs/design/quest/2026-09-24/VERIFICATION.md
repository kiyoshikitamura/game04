# 出陣再仕上げ・検証記録

## 判定

**Git保存・専用Preview配信済み。ビジュアル完全受入としては未完了。**
配置・正式情報・接続・読み込み抑止・中央Dialogを再仕上げし、独立担当の表示差戻し7件を修正した。ただし、下記素材の対応不足とモック意匠との差を残しており、以前の仮クローズを引き継いで完成とはしない。

## 保存と配信

- Branch: `work/game04-quest-finish-20260924`
- 最終コードSHA: `648ad12337a5f6e5c1ebb5e35c26d4672ddee4d6`
- 親SHA: `c9a7104fa04a103d41b4b28c8df6a4e811a2e6df`（5画面・実API・低画面の主検証版）
- Draft PR: https://github.com/kiyoshikitamura/game04/pull/26
- Production公開、Production環境設定変更、mainマージ、DBマイグレーション、Edge API配信は実施していない。
- Preview識別情報は `/qa/quest-live-viewport` の表示とGitHub Vercel statusで照合。最終情報は `DEPLOYMENT.json`。

## 本体実操作・実API

QA専用新規ユーザー「出陣検証0924」を通常UIで作成。実API本体 `/` をPreview専用の寸法枠iframeで表示した。fixture/state注入を使わない。

| 操作・状態 | 結果 |
|---|---|
| エリア→ステージ→挑戦前 | 三河3ステージ、1-1のみ初期解放。正式女侍・土・Wave1・初回消費0 |
| ヒント・報酬 | 正式内容、戻る操作、初回獲得済表示、通常・レア報酬表示を確認 |
| 出撃準備・武将詳細 | 所持5人、順序、正式Lv/HP/属性/レアリティ、共通SP0/400、装着スキル/未解放枠 |
| 編成変更往復 | 共通武将画面でくノ一を2番へ移動し保存。伝令1番で同ステージ準備へ帰還 |
| キャンセル | 挑戦前へ戻る。行動力50を維持 |
| 画像準備 | 読込中の出撃/編成/詳細を非活性化。完了後に解除を実画面確認 |
| 初回出撃ダブルクリック | 1戦闘、1決済、銭2600→3100、行動力50→50、1-1clear/1-2解放 |
| 再読込 | クリア、1-2解放、変更済編成、銭3100、行動力50保持 |
| 消費あり再挑戦ダブルクリック | 追加戦闘1件、行動力50→45、通常銭500、初回券/武将の再付与なし |
| 再挑戦後再読込 | 銭3600、行動力45、クリア・編成保持。以降自然回復で46を観測 |
| 結果→帰還 | 2回とも勝利・結果表示、ステージ一覧へ帰還 |

DBは読取照合のみ。戦闘行・requestId・消費・報酬・進行は `LIVE_DB_READBACK.json`。
直接HTTP検証スクリプトはネットワークtimeoutで未完走（`LIVE_API_VERIFICATION.json`）。ブラウザ経由の上記実API成功と区別する。任意HTTP同requestId再送、実ユーザーの行動力枯渇/不足編成による拒否は実APIでは未実施。

## ドメインと実ハンドラーの隔離検証

- `DOMAIN_VERIFICATION.json`: 既存 `verify_game04_quest65.ts` の全assertion実行。正式65面/356敵、入力一致65、APIハンドラー入力照合65、進行65、初回/失敗後/周回コスト、固定報酬、seed安定、保証境界、旧ID保持。transportはメモリ内fixture。
- `GUARD_FIXTURE_VERIFICATION.json`: 保存済Edge handlerを無変更で実行。未解放・行動力不足・4人編成は400拒否、commit0、state不変。決済済同requestIdは保存結果返却、commit0、state不変。ネットワークfetchは全置換され、実API検証ではない。
- `scripts/verify_game04_quest_guards_20260924.mjs`: 上記4件の再現スクリプト。
- TypeScript型検査PASS。Preview設定でWebpack production build PASS。最終CSSのみの追加修正もPreview buildの成功を確認。

## ビジュアル・操作

- 390×844本体5画面、ヒント、報酬を撮影。300px（モック各画面相当）でも5画面を撮影。
- 375×640本体で挑戦→準備→詳細、内部スクロールでスキル表示、戻る→キャンセル→閉じるを確認。Header/FooterとDialogを重ねて操作させず、金色scrollbarと固定末尾CTAを確認。
- 独立fixture照合の札・番号重なり・横overflow・エリア名・詳細外寸・属性可読性の差戻し修正は `INDEPENDENT_REVIEW.md`。
- 最後に300px比較で補助CTAの改行を親が検出し、`648ad12`でnowrapと狭幅文字サイズを修正し、配信後300px本体で非改行を確認。
- 本陣/武将/共闘の遷移とHeader/Footerは本体で確認。共通ボス表示は出陣variantのみ変更しdefault保持。本陣/共闘本文の旧名称は他ラインの既存成果範囲で、今回一括改変していない。

## 比較画像

- `comparison/mock-vs-live-300.jpg`: 上段モック、下段本体。上段の仮値/旧名称は採用しない。挑戦前は最終648ad12、他4画面はc9a7104（同コード）で撮影。正式初回クリア後状態で撮影しているため進行・消費値の差は意図的。
- `comparison/live-five-390.jpg`: 本体5画面。親版c9a7104。
- `comparison/dialogs-and-low-viewport.jpg`: ヒント/報酬/低画面詳細と内部スクロール。

## 未解消事項

1. 既存エリア背景の割当が地名と一致しない。三河は雪の春日山城、尾張は青葉城。正式エリアID→背景の承認済対応が確認できておらず、今回既存の割当を保持した。対象10エリアの対応を確定する必要がある。
2. モックの細密な角飾り・錠前・朱CTA装飾に対応する正式素材の割当がない。文字/既存枠で機能は表示するが、意匠一致としては未達。
3. 正式カード1060:1484比率・追加クロップ禁止を優先したため、出撃準備の人物占有率はモックの細長い帯より小さい。独立情報領域を確保し変形はしていない。比較補正と残差を区別して記録。
4. 実HTTP再送/行動力不足/不足編成は隔離ハンドラー検証止まり。DB競合までの実環境検証とは主張しない。

ユーザーに差分発見を委ねず、上記を担当側で検出して記録した。現段階を完成・受入済みとは扱わない。
