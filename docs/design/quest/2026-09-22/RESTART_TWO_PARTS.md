# 2部品での再開
状態：実装候補を追加。型・Build・実画像・演出の検証は未実施。未承認、本体未展開。
基準SHA：d8c534b1d283ce3b6843b4fd139fb09550e6150e
本Commitはカードとボス展示の実装候補、同じ部品を使うQAページを追加する。既存クエスト画面は変更しない。

## 対象
- src/app/components/redesign/visual-bench/CharacterDisplays.tsx
- 同階層 CharacterDisplays.module.css
- /qa/visual-parts
2部品を親実装でそのまま利用する。モックに似せた別のQA専用部品を本体へ作り直さない。

## 候補で明示した寸法
- 単体カード幅192px、画像3:4、人物は内側7%/8%/9%に収める。
- ボス展示は幅最大358px、画像領域340px、人物領域は左右20px・上16px・下12px。細いカード枠へ押し込めない。
- 背景/人物/枠/バッジ/文字を別領域へ分離。
- 透明余白をブラウザで測り、SVG viewBoxで人物の実際の範囲をcontain表示する。元画像ファイルは加工しない。
寸法は承認済みモックを実装へ落とすための候補。実画像を見て調整し、調整後の値を確定する。計算だけで合格扱いにしない。

## 実データ
現在のCHARACTER_MASTERSから選択しN/R/SR/SSRを確認。所持・編成・保存データへの書込みはない。
用途別画像と武将固定背景の対応表を厳密に参照。欠落はエラー/未割当表示で可視化し旧画像へ代替しない。
SRは光沢、SSRは光沢と内側の光。非表示タブ・低減モーションへ対応する候補。読み込みは必要画像が揃ってから部品をまとめて表示。
ボス部品の背景は今回武将固定背景を使う表示確認用。本体接続時に戦場背景仕様を確認し、未承認の背景ルールを広げない。

## 次の実装担当の作業
1. この候補Commitを最新実装へ必要差分だけ取り込む。正本数値・共通UI・既存進行を保持。
2. Typecheck/Buildを実施し、MENU→QAへこの確認ページへの入口を追加する。既存QAガードを保持し、Productionでは公開しない。
3. 正しい画像が表示された390px/375pxのカードと単体ボスを確認。N/R/SR/SSR各1件と、武器の長いボスを確認。実体の外接矩形が人物領域へ大きく収まるか、カード枠の開口部、透明余白、顔、属性、背景、文字を目視する。
4. 動画または操作でSR/SSR差、低減モーション、非表示タブ停止、遅延/失敗時表示を確認。結果画像と動画をGitへ保存。
5. 2部品の表示が一致するまでクエスト全体の再修正はしない。確認後に挑戦前1画面へ接続し、その後に編成・詳細へ展開する。
6. カードの小さい5人横並びとボス複数体のレイアウトは後段。本候補をそのまま縮小して本文を小さくしない。
Production公開・mainマージ・GAME03変更はしない。配信・統合は親担当へ集約。

## 検証の限界
この作成環境ではGit読み取り/書き込みは可能だが、Previewへの直接通信がタイムアウトし、フルリポジトリの実行環境もない。表示未確認のため完成とはしない。従来の問題を繰り返さず、上記の実画像検証が済むまで未検証として保持する。
