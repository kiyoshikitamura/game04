# P03 認証移植・検証記録
2026-09-24

## 採用元と所有
- GAME03 read-only: 0453fcda56c2f3546b91eb7a073c592988f9cf42。TutorialAuthentication.tsx の Google linkIdentity / email updateUser(email)→確認→updateUser(password) を再利用。
- GAME04 base: 7476566f945708a8e752f71d4d6b90c23f3b937a。
- Branch: work/game04-p02-p04-20260924。
- P03所有: src/app/auth/game04/**, src/app/api/auth/game04-binding/**, src/utils/authGame04.ts, src/utils/oauthReturnSession.ts(新callback判定のみ), supabase/candidates/game04_p03_auth_binding.sql。
- GameContext、RedesignApp、ゲームAPI、共通商店/設定は未変更。G2統合未完。

## 接続契約
実DBを読み取り、users.id / game04_player_state.user_id / user_account_auth_methods.user_id,auth_method,authenticated_at を確認。
Supabase検証済みUIDが同一の既存playerを所有することが必須。プレイヤー新規作成、email文字列による統合、資産・報酬・tutorialの更新は行わない。
game04_auth_binding(boolean)は候補SQL、未適用。auth.uidで所有確認、auth.users/identities実体で匿名・単一方式・メール確認・password有無を確認。finalize時はmethods登録のみ。既存方式が異なれば拒否。全identity数と許可providerを別判定し、同時insert後もmethod再読取。
P02はbinding必須、同UID player、単一provider、email confirmationを照合する。旧complete_tutorial_authenticationはreward/旧tutorialへ波及するため呼ばない。

## 導線
独立入口 /auth/game04。Google新規連携はlinkIdentity、既存ログインはsignInWithOAuth。
メール方式はパスワード方式のまま。匿名UIDへメール確認→同UID password設定→binding。パスワードは永続保存しない。
専用callback /auth/game04/callback。自動URL session受入を停止し、original guestを確認してから既存acceptOAuthReturnを使用。intent UID不一致はbinding停止、元session復元（保持している場合）。旧置換/削除RPCは呼ばない。
メール確認別ブラウザはintentがなくてもサーバーの既存UID所有と確認済みidentityで続行。Googleの未連携ゲストは外部ブラウザへsessionをコピーしない。連携完了後は同一認証方式で別端末ログイン。
戻り先は同origin固定。P06本番決定前の誤接続を防ぐためbridgeはGAME04 devのみ許可。

## 検証結果
|項目|結果|
|---|---|
|実DB table/column read照合|確認済|
|GAME03方式読取照合|確認済|
|独立コードreview|単一identityチェックと競合後method再確認を修正|
|候補SQL適用|未適用、G2親所有調整待ち|
|Google実認証成功/重複/取消|未確認|
|メール実送信/確認/再ログイン|未確認、許可された確認先が未指定|
|ゲーム資産維持の実操作|未確認|
|型check/build/browser|未実施、実行環境がenvironment_offlineに切断|
|専用Preview|親担当、P03では未確認|

## G2 統合
設定・未認証購入案内のリンク先を /auth/game04 とする差分をG2が統合。フルページ移動後同一Supabase UIDでGameContext/bootstrapを再読込。認証成功だけで旧tutorial/auth報酬を発生させないことをG2回帰確認。G4初回ゲスト作成は既存ユーザー保持のまま別工程。
SQLは共有DB所有調整後に親が適用し、匿名・多重provider・別方式・state無し・email未確認/password無し・同UID再実行を確認。未適用の現在はAPIがfail-closedで連携完了を返さない。

## P06設定表
|設定名|用途|環境|担当|状態|
|---|---|---|---|---|
|NEXT_PUBLIC_SUPABASE_URL / ANON_KEY または PUBLISHABLE_KEY|GAME04認証接続|専用Preview|親/P06|既存値の配信先照合要|
|Supabase Auth redirect allowlist|/auth/game04/callback|専用Preview|親/P06|設定確認待ち|
|Google provider / client設定|Google認可|GAME04 dev|親/P06|確認待ち、GAME03変更禁止|
|Google authorized redirect URI|https://lrgyllgzcdcphlbmkknc.supabase.co/auth/v1/callback|Google console|P06|確認待ち|
|Email confirmations / SMTP sender / templates|確認メールとpassword方式|GAME04 dev|P06|確認待ち|
|許可済みテストメール宛先|専用受入|Preview|ユーザー/親|未指定|
|本番認証domain・allowlist・公開環境|正式戻り先|Production|P01/P06/M|本番設定待ち|
|game04-binding project制約|本番接続への切替|Production|P06/M|本番先決定後に明示更新|

## M受入
Google/メール各々の成功、再ログイン、取消、期限切れ、重複identity、ログアウト、端末/ブラウザ移動、同UID資産前後比較、購入binding、問い合わせ戻り先、正式domainの戻り先。検証環境受入済み/本番受入済みとは報告しない。

## 独立レビュー後修正
- Googleまたはメール認証済みだがbinding確定が一時失敗した場合、確定を再試行するボタンを追加。失敗状態を連携完了と扱わない。
- ゲストが既存認証に衝突した場合、明示的に既存アカウントへログインする導線を追加。元のゲストセッションは同一タブのsessionStorageだけへ一時保持し、復帰ボタンで復元できる。セッションをURL/ログ/資料へ出さず、資産統合・削除は行わない。
- 画面のscroll ownerをheight:100dvh + overflowY:autoで固定。操作ボタン44px以上。
- React best practices skillに沿いhooksの条件分岐/多重submit/label/状態通知を確認。環境offlineにより型・build・実ブラウザの合格とは扱わない。
- auth_method uppercaseは実DB CHECK(EMAIL,GOOGLE)、user_id PKとusers FKをread-only照合済み。

## P06 cron / G2・G4資産保持への引継ぎ
P06参照: work/game04-p06-20260925 / 6ee3a2af7f298c889d797c0e25a077bf2d80cba0 / docs/P06_DB_API_STORAGE_REPORT.md。
実DB関数cleanup_expired_anonymous_onboardingをread-only確認。匿名true、旧tutorial未COMPLETE/AUTHENTICATION、24h超、methods無し、非anonymous identities無し、最近のsession refresh無し等が対象。
P03で認証完了したUIDは非匿名・methods・非anonymous identityにより除外され、旧tutorialを更新しなくてもこのcleanup対象にならない。
一方、未連携のGAME04 game04_player_stateは除外条件に明示されていない。集計では新state保有匿名45件、うち旧tutorial未完45件。これは24h等の全条件確認前の母数であり削除対象確定件数ではない。
G2/G4へ最小候補: AND NOT EXISTS (SELECT 1 FROM public.game04_player_state g WHERE g.user_id=au.id)。保持期間/休眠ユーザー方針は担当が決める。P03は共有関数/cronを変更していない。P06本番cron採用は資産保護の受入後まで保留する。
