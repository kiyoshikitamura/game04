# 本陣・独立検証記録

状態：静的検証・実API記録・本体4状態・低画面Dialogの独立目視照合を完了。親の実操作記録を以下に分けて記載。最終配信の識別情報は親の配信記録を参照する。

## 静的レビューと担当への差戻し

| 指摘 | 対応/確認 | 区分 |
|---|---|---|
| 既存UI背景IDとAPI受付IDが不一致 | `home.ts` へ統合、旧 `bg_default` / `bg_kabukicho` と現行城背景IDを解決。APIは共通選択検証を呼ぶ | ソース確認 |
| 未解放背景と武将の同時変更で部分保存しないこと | `applyHomeSelection` は全項目検証後に新stateを返し、失敗時元state不変 | ソース確認 |
| 背景10エリア対応の捏造禁止 | `QUEST_AREAS` の既存画像・先頭ステージ解放判定を共用 | ソース確認 |
| 共闘候補の期限切れ優先で有効通知が隠れる | `RedesignApp` の候補選択に期限判定を追加。時刻更新による候補再選定も接続 | ソース再確認済み・実状態待ち |
| 下部活動行の顔・時刻・右操作が欠ける | 表示担当が既存 `get_public_profiles` と正式actor ID、created_at、DM導線を接続 | ソース再確認済み・実画面待ち |
| 同盟のロック表示がない | 表示担当が非活性ボタンへ機能状態のロック記号を追加 | ソース再確認済み・実画面待ち |
| 準備中に下部ボタンが操作可能 | 主要/交流/バナーdisabled追加。共闘確認とDM会話一覧も同じ制御へ追加 | ソース再確認済み・実操作待ち |
| バナー人物が読込ゲート対象外 | banner cardを追加し、画像onload後のdecode完了まで待つ | ソース再確認済み |

## 実行環境の確認

- 本体入口は `/` の `RedesignApp`。`/qa/redesign` はfixtureであり、実API保存確認と区別する。
- 既存共闘検証の最終記録には390×844／1536×936の実寸iframe方式がある。
- 既存専用QAのセッションファイルは利用可能。初期確認時はaccess token期限切れ、refresh tokenあり。秘密値を出力・コミットしていない。
- 独立検証担当のツールには、control-browserスキルが要求するNode REPL `js` とtool discoveryの呼出ツールが公開されていない。スキルを読了し、親へ担当調整を依頼した。非対応の経路からブラウザを操作していない。

Typecheck/Build/HTTP応答・ソースレビューだけを見た目や本体実操作のPASSと扱わない。

## 接続担当の実API記録レビュー

`live-api.json` と `scripts/verify_game04_home_live_api.mjs` を独立担当が読取確認。
GAME04 dev API v18、既存使い捨てQAに限定した次の実行結果を確認した。独立担当による再実行ではない。

- 武将・背景の同時保存と `get_state` 復元。
- 同一requestId再送で選択とversionが不変。
- 旧2IDと既存正規2IDの保存・再読込。
- 未解放背景＋別武将の同時変更を原子的に拒否し、保存値とversionが不変。
- 不正背景ID・未所持武将の拒否。
- 編成・育成・所持品・銭・輝石・出陣進行の保持。
- 検証後は元の表示へ復元。元が未設定だったため、キー不在への復元ではなく同じ表示になる既定値の明示保存と記録している。

これは認証付き実APIの証拠であり、ブラウザ操作→描画→再読込の証拠を代替しない。

## 第1回・本体390px画像の独立照合

親担当がブラウザで撮影した `home-normal-first.jpg` と `home-switch-first.jpg` を独立担当が原本JPEGと目視比較。
判定：要修正。担当間で直接差戻し済み。

| 状態 | 差分 | 修正担当 |
|---|---|---|
| 通常 | Header顔が小さく、顔画像としての占有率が不足 | 親 |
| 通常 | 背景が交流・バナー込みのページ全高にcoverされ、城の右端が切れ、人物の足元が石畳でなく屋根・木になる。原本は城・左右灯篭・石畳を同時に表示 | 表示 |
| 切替 | Dialogがほぼ全高になり、灰色の不透明面が強く、原本の黒透過・桜装飾が消えている | 表示 |
| 切替 | 選択カードの金縁・外光が弱い。候補名の可読性を含めCSS競合を確認 | 表示 |

人物の靴等は既存正式画像との差であり、再生成対象にしない。人物の全身占有率・通常のCTA並列は概ね成立。
Dialogの所持5体・背景12件は正式データ差、保存CTA追加は保存要件のため許容。モックの3体へ固定しない。
`home-lower-first.jpg` も追加照合。システム救援文の「レイド」表記残りと、大型の共闘確認CTA/丸DMによる活動行の過大高さを表示担当へ差戻し。保存本文やユーザー自由投稿は変更せず、システム表示文だけ名称を統一するよう依頼した。表示担当が小型リンク・DM操作・正式プロフィール名・システム表示名へ修正し、独立担当はソースを再確認した。第2版画像での再判定待ち。

共闘発生・低画面Dialog・実操作後復元の画像/操作記録はこの時点では未確認。

## 第2回・共闘発生390px

`home-encounter-v2.jpg` を独立目視。専用QAへ正式Masterから用意した共闘roomを実APIで表示した本体画像。自然遭遇発生の通し証拠とは区別する。

- 背景の過大拡大を解消。城全体・左右灯篭・足元の石畳が同時に表示される。
- Header顔の占有率が改善し、Lvと顔が判別できる。
- 人物下・CTA上に別領域で通知。「共闘発生／柴田勝家／残り9分16秒／確認」が折返しや重なりなく収まる。
- 左導線、主要2CTA、Footerは承認構成に沿う。

判定：この状態の重大な見た目差は解消。正式人物素材・通知値・正式既存アイコンの差は既知差として許容。対象ID/期限連動は操作記録との照合を別途行う。厳密な画素一致を主張しない。

## 第2回・切替Dialog390px

`home-switch-v2.jpg` を独立目視。中央配置・背面暗転・黒透過面・金枠・選択カード外光・末尾CTAへの到達性は第1回の差分を解消した。
所持5体のため背景一覧が内部スクロール下に続くことは正式データ差として許容。
桜装飾は `100% 100%` による縦引伸しが残るため、縦横比を維持するよう親から表示担当へ再差戻し。修正後画像で確認する。

親の本体実操作報告：通知の「確認」から柴田勝家Lv1／主催「本陣QA924」／HP39,000／残り9分の該当詳細へ直接遷移。通知対象と詳細表示の一致を確認したとの報告。独立担当自身のブラウザ操作ではないため、最終提出では親の操作記録と合わせて参照する。

## 4状態の比較画像

`create_comparison.py` で原本を幅390pxへ比例縮小し、同じ幅の本体画像を無加工で並べた。原本の高さを切らず、比率の差は余白で示す。原本画像や本体スクリーンショットへ修整は加えていない。

| 状態 | 本体証拠 | 比較画像 | 独立目視判定 |
|---|---|---|---|
| 通常 | `home-normal-final.jpg` | `comparison-normal.jpg` | 背景/人物/左導線/2CTA/Header/Footerに重大な崩れなし |
| 共闘発生 | `home-encounter-v2.jpg` | `comparison-encounter.jpg` | 通知は人物と分離、対象/残り時間/確認が表示され重なりなし |
| 同一ページ下部 | `home-encounter-final.jpg` | `comparison-lower.jpg` | 活動密度・小型操作・システム名称を修正。CTA/薄型バナー/Footerに重なりなし |
| 統合切替Dialog | `home-switch-final.jpg` | `comparison-switch.jpg` | 桜引伸しを解消。中央暗転/選択枠/内部スクロール/固定末尾CTAが成立 |

一覧は `comparison-four-states.jpg`。本体下部は共闘通知ありの状態。QAプロフィール顔の未設定、活動文、人物の正式原画、所持5体・背景12件、数値、Footerの正式アイコン、明示保存CTAはデータ/確定要件差として区別する。
原本と本体の縦横比が異なり、人物表示高とDialog内の見える候補件数にも差がある。厳密な画素一致・全ての状態を同一配信SHAで再撮影したことはこの画像比較だけでは証明しない。配信SHAと最終コードの関係は親の配信記録を参照する。

## 低画面390×568・最終操作記録

`home-switch-low-final.jpg` / `home-switch-low-bottom.jpg` を独立目視。上下の閉じる/保存CTAが画面内に収まり、背面は暗転。内部だけを末尾「関ヶ原」までスクロールでき、金色バー、未解放背景の非活性表示、固定末尾CTAを確認した。Header/FooterにDialog操作が隠れない。

親担当のブラウザ操作報告（独立担当自身のブラウザ操作とは区別）：

| 区分 | 確認した操作・結果 |
|---|---|
| 最終本体 `5dc1c98` | 390×568で末尾まで内部スクロールし固定保存をクリック。Dialogが閉じ、本体へ反映 |
| 最終本体 `5dc1c98` | アカウント連携から既存Google/メールのフォーム表示・閉じるを確認。認証送信は行っていない |
| 実API本体通知 | 専用共闘の期限 `17:59:44Z` に通知が消えることを実観測 |
| 保存失敗fixture | ダブルクリックで attempts=1 / pending=1。失敗後はdraft保持・保存値不変。再試行後 attempts=2 / success=1、伝令 / castle-town反映 |
| 実API保存 | `live-api.json` の同時保存/再読込・原子的拒否・再送・資産保持を参照 |

保存失敗fixtureは表示と連打境界の証拠であり、実API障害を起こした証拠とは扱わない。4状態と低画面画像に、担当へ差戻すべき重大な未解消の崩れは見つからなかった。

## 最終通知境界・親操作記録の追補

親担当は専用QA fixture roomをdefeated/HP0かつ未来期限へ一時設定し、本体を再読込。homeReady=true / notificationVisible=false を確認した。終了状態による除外を期限切れとは独立に確認。検証後は元期限へ戻し、資産/報酬は変更していない。正のDM未読配送は作成しておらず、既存useChatの件数取得とmark_direct_message_read呼出経路の接続は静的確認に区分する。

## DM残検証の最終クローズ（2026-09-24）

ユーザー承認後に親担当が専用QA間で実施したDM未読試験を追補。上記の『正のDM未読配送は未実施』は、この追補で解消。

独立担当はhome-dm-unread-20260924.jpg / home-dm-read-20260924.jpg / home-dm-restored-20260924.jpgを目視確認。未読1件→会話Dialogの送受信2行と未読解除→再読込後の未読0・会話保持が、親の操作報告と整合。追加修正・再試験は不要と判定。

初回受信はSQLから正式RPCを実行したQAデータ投入、本体返信はtextbox/送信操作であり、混同しない。DBのID/is_read・再読込操作順は親の実行記録を根拠とし、独立担当がDB実行したとは記載しない。詳細はDM_VERIFICATION.md。
