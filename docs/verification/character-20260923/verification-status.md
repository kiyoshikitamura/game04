# キャラ配下 独立検証（2026-09-24 再開）

状態：未完了。以下は QA fixture とドメイン検証であり、本体実 API 保存・再読込検証の代替ではない。

## ドメイン

- `node scripts/verify_game04_character_growth.cjs`：PASS。編成枠 3 / 4 / 5、最大 5、保護・装備中の分解拒否、魂交換の最低 10・偶数、実結果差分、正式装備 160 件、成長曲線、LB による能力値二重補正なし。
- `node scripts/verify_game04_character_boundaries.cjs`：PASS。正本 EXP・銭 800 セル、上限・不足時非消費、汎用魂併用、初回解放の汎用魂禁止、重複解放拒否、スキル不足・上限、同一装備個体の二重装備拒否、JSON 保存構造整合。
- 追加：覚醒・装備 LB の結果で現在 Lv 不変を明示。同状態を再送結果に渡しても `changed=false`。

## QA fixture のブラウザ

- 共通の実装部品 GrowthView を使用。`/qa/redesign?view=character`。保存・実 API を使用しない。
- 390 × 844、390 × 600。デッキ・キャラ一覧・詳細・Lv 育成・結果・スキル一覧/詳細・装備一覧/詳細。
- Lv 素材小 1 個 → Lv 1 から 3 / EXP 0 から 100 / 銭 30,000 から 29,940 の実 fixture state 差分を結果 Dialog へ表示。
- JavaScript pageerror 0、横方向スクロール溢れ 0。
- 画像読み込み完了待ちを強化して再撮影中。初期画像に「読み込み中」が写るものは受入証拠として使用しない。

## 表示担当へ差戻し済み

- デッキ人物が小さく、カード内の文字折返し、出撃デッキ見出し・選択中スキル領域の欠落、おまかせ 3 列配置、装飾・余白がモックと相違。
- 能力値の 3 列 + 余りセル、結果で名前・人物がない、ATK / DEF 表記、スキルの内部向け開発説明が残存。
- 最大 Dialog 高さが 80dvh ルールと相違するため親へ共有。

## 未検証

- 本体専用 Preview の同状態比較と実 API 操作・保存・再読込。
- 全操作の不足・上限・キャンセル・連打・再送ブラウザ確認。
- 共有 Header / Footer / Modal の出陣・共闘への回帰確認。
- 訂正済み比較基準画像との最終並置比較。
