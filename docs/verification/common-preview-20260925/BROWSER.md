# 本体接続の限定ブラウザ確認

2026-09-25 / cloud Chrome / 実API・隔離DB。全画面再監査・完了済み性能試験は反復していない。

- 共通固定URL（2fd350a）: Ready、ルートのTAP TO STARTを表示。
- 接続通し操作（f312663の固定Preview、API v5）: TAP TO START → はじめから → 世界観3場面 → 秀吉案内 → 正式R3体 → 正式技能3種 → おまかせ編成 → 固定模擬戦 → 名前保存 → 初回本陣 → 出陣 → 1-1実戦闘 → 勝利。
- 模擬戦前にreloadしTAP TO START → 続きから。同じ模擬戦前の場面を復元。
- 初回本陣は出陣案内を表示。DBでも tutorial.step=17、loginEligible=false、user_login_bonuses=0 を確認。証拠 first-home.jpg。
- 固定模擬戦は通常戦闘から独立した承認済み台本を使用。結果へボタンから名前入力へ接続。
- 本編1-1はAPI実戦闘が成功し勝利表示。API別QAでも育成/編成/無料10連/2回目ホームログボ/再取得保持を確認。
- 最終差分: 承認名称表示・救援の明示ID/現在Lv表示。チュートリアル遷移、初期付与、戦闘・保存APIには変更なし。

ユーザー実機の折返し・タッチ/音声・回線/端末・全残件の最終受入は未実施。Preview提出はG5合格ではない。

通常戦闘の結果から本陣へ戻り、初回ログボ表示を確認。閉じた後、通常任務「1-1を初クリア」を開いて報酬受取。受取済み1件・銭+1000を確認（normal-mission-claimed.jpg）。
