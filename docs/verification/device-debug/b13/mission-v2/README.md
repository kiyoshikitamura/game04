# DBG-053 改訂案2（未承認）・共通密度ルール

2026-09-26 ユーザーの再提案指示。旧mission-proposal.htmlは不採用の履歴。現案はproposal.html、実任務のMissionContentは変更しない。

- 上段：任務名16px＋進捗数14px。下段：報酬24pxアイコン・数量＋右96px操作。受取済みはチェック（読み上げ名あり）。進捗ラベル・状態バッジ・進捗バー・全幅操作を除去。
- 外枠内は区切り線の行。見出しは既存任務バナーを6:1のまま240px幅、タブ44px、閉じる96×44px、本文だけスクロール。ダイアログ上限80dvh、行は可変高。
- 375/390×664の通常／達成未受取／全受取済／長文多数を保存。通常は完全表示3件（4件目も一部見える）、通常行93px。長文6報酬は125〜149pxに伸長。全文・全報酬を切らず末尾まで到達。操作は44px以上、横はみ出しなし。
- 報酬・進捗・長文はレイアウト検証サンプル。実装承認後は正式マスターの全報酬・名称・条件を保持し、任務/報酬仕様を変更しない。実受取・挑戦処理はこの見本に接続しない。
- 共通CanonicalDialogに明示指定density=compactを追加。既存standardは維持。ログインボードに適用し、見出し/本文/操作域の余白を縮小、閉じるは右寄せ。30日枠はgridの引伸ばしを止め、報酬数に応じて自然高。2/3/4報酬で約111/139/167px、長い内容は折返しで拡張。
- ログインは375/390、30日全表示データ、本文末尾到達、閉じる、pageerror0を確認。報酬/受取日/付与処理の変更なし。全68面の監査は行っていない。

再生成: node scripts/build_b13_mission_v2.cjs。限定検証: モックのlocal:3013起動後 node scripts/verify_b13_mission_v2.cjs。証拠verification.jsonおよびPNG。端末実機の受入は未実施。
