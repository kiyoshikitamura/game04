# B03 共通UI 一括提案・第4案（未承認）

基準：PR #37 head 4e34bd88815dbf28c4f3ae5c773a7318d8bcbee4。直前配信 d9be130。375/390×664px見本は review.html / result-v4-375.png・result-v4-390.png。proposal.pngは初回案の履歴。本文の指示に従い、新しいデザインは共通Previewへ配信していない。

## 一括承認を求める設計

|対象|提案する具体的な規則|
|---|---|
|003・019 読み込み|共通 LoadingState に startup / inline を持たせる。開始時は初めからロゴ幅230px・28pxスピナー・14pxテキストを中央、枠なしで一枚の面を維持。短い局所ロードは150ms後に28pxスピナーのみ。2秒を超えたら同じ予約領域内でテキストを追加し、ロゴは全画面の場合だけ追加。局所ダイアログにタイトルロゴは出さない。短時間で消える場合は待たせる最低表示時間を作らない。長時間化でもコンポーネント再マウント・画面暗転・文字の順次差し替えをしない。失敗時は再試行へ。|
|022 チュートリアル文字|390×664で人物420px・下部244pxを予約。本文14px/1.65、本文104px、名前行・CTA48px。高さの低い端末では人物領域が残り高さを受けるが、文字数によって人物を縮めない。本文だけ内部スクロール、金色5pxバー。全文の領域を最初から確保、タップ全文表示を維持。入力場面は入力フォーム専用の領域を使う。|
|005 武将|共通 CharacterCard を表示の正本にする。人物の既存サイズ・切り抜き範囲を維持し、背景だけ枠外周の裏まで塗る。キャラページ・出撃・獲得の差は外側サイズとメタ情報の有無だけ。レアリティ別の承認済み枠・背景を利用。スキルは画像そのままで枠背景なし。|
|014 勝利|戦闘全体とナビを置換。ステージに対応する承認済み背景を全面表示し、編成先頭武将を腰上で大きく表示。上部22pxにステージ名と64pxの「勝利」、人物は140px以下の領域に配置して顔と文字を分離。下部固定CTA「報酬へ」、報酬詳細は次画面。先頭武将はMVP扱いしない。武将ごとの透過余白を計測する既存Personを利用して頭部と腰位置を合わせる。見本の北条氏康は腰上構図。|
|015 共通ダイアログ|現状は Modal と CanonicalDialog の2系統。共通 DialogFrame へ寄せる。幅min(358px,画面幅−32px)、最大80dvh、ヘッダー固定、本文min-height:0・内部スクロール、フッター固定。高さはauto、最大80dvhは上限。短文のご案内は内容に沿う高さ（390幅の見本で約220px）。大きな「出陣」の重複見出しを削除し、本文とCTA「出陣へ」に集約。長文時だけ本文が伸びて上限でスクロール。主CTA48px・幅100%、副CTA44〜48px・同幅で縦積み。本文14px、タイトル20px、余白16px。ナビより上のポータル層。フォーカス制御・Escape・閉じるを保持。|
|020 挑戦|BOSSラベル、ボスを約290pxの領域いっぱいに配置。名前・属性・小さいHP/ATK/DEFを下部グラデーション上へ重ねる。第N派・行動力を一行、ヒント/報酬を各44px以上の副操作に。本文だけスクロールし挑戦CTAを隠さない。ヒントは任意表示。|
|030 報酬|勝利から進む専用結果。通常/初回の報酬カード、対象行だけ金色「初回」バッジ。正式 stage.firstRewards と settlement.firstClear で出所を区別し、同種報酬の通常分を初回扱いしない。PLAYER EXPは増加量・現在/到達Lv・進捗バーの共通GrowthSummary。計算済み playerGrowth を表示するだけで再計算しない。次ステージを主CTA、一覧を副CTA。未解放・最終ステージでは一覧を主CTAに。|
|024 第N派|単一派を含め毎回、150msフェードイン→650ms保持→200msフェードアウト→敵200msで登場。計1.2秒。中央64px、既存背景は消さず、導入中は再生時計を停止してHP/SPを進めない。スキップ・一時停止・復帰時の二重演出を防ぐ。reduced-motionは静止表示と切替へ。表示文言は「第1派 / 全3派」、事前情報は「第1派〜第3派」。内部waveキー・保存snapshotは変更しない。|
|026・029 戦闘配置|味方5列・各3スキルは縦3段、各スキル44px高・横幅約66px（375幅）/69px（390幅）、段間4px、列間6px。画像30px・優先番号・詳細ラベル、武将名11px、顔60px、HPバーと現在/最大値。スキル画像の装飾枠は付けず操作ボタン枠のみ。各アイコンは発動でなく既存の詳細表示を開く。味方の必要高を先に予約し敵領域を残り高へ。664px高で敵267px、ヘッダー48px・SP44px。1体中央幅最大300px/2体均等2列/3体均等3列、画像は承認済み比率contain・足元揃え。敵の名前/HP/行動カウントを同じ敵に重ねる。幅375/390×敵1/2/3の計6条件、15ボタンずつ90タップで対応詳細の表示を確認。iOS実機の指操作は未確認。|
|017・027 エリア|出陣一覧背景に既存 castle-approach.jpg を提案。個別エリア背景は既存承認済み画像を維持。バナー高さをd9be130の80%（幅同一・aspect1.52→1.9、min205→164）。右端に最終ステージ最終派の代表ボス、左にエリア名と進行表示。既存 selectRepresentativeBoss と同じ boss指定優先/レベル/HPの規則。見本は正式マスタより三河1-3直江兼続、尾張2-4徳川家康、美濃3-5織田信長。攻略文の常時表示は復活させない。|

## 確定修正と承認後の実装

確定修正は別途ソースへ反映。提案レイアウトはreview.htmlのみで、アプリに未接続。承認後に上の共通部品と表示を組み込み、対象箇所の型/ビルド/限定ブラウザー検証、Git保存、PR37統合、共通Preview配信を実施する。実機受入までは解決にしない。

報酬やEXP見本の「正式値」は配置ラベルであり数値変更ではない。敵配置見本の武将は配置説明用。B03原画像16点は保存済みIDのみ受領、原画像未取得・独立閲覧なし。既存承認済み素材と本文を使用した。

## 第2案の操作確認と準備範囲

scripts/serve_b03_review.cjs でローカル見本を起動、scripts/verify_b03_review.cjs で375/390pxのタッチエミュレーションを確認。review-v2-checks.jsonに寸法と結果を保存。静的な配置見本に詳細ダイアログを加えたもので、本体への適用・配信ではない。初回3スキル検証evidence-b03は上書きしていない。

承認後の共通部品接続順：LoadingState（startup/inline）→ TutorialTextPanel（固定本文と文字送り）→ CharacterCard利用箇所集約 → DialogFrame（案内/挑戦/報酬）→ BattleParty/EnemyFormation/WaveIntro → VictoryScene/RewardSummary/GrowthSummary → AreaBanner。既存の保存済み戦闘記録、再生時計、画像ゲートと成功済み12件を維持し、ゲーム数値・DB・APIは変更しない。

全18件の状態：009/012/023/025/028は修正保存済み、005/019/024/027は確定差分保存済み＋残設計、003/014/015/017/020/022/029/030は設計案提示、026は既存検証と今回の提案検証を別記。承認後に本体へ組み込み、同原因の限定回帰を行いPR37へpush・共通Previewへ配信・DBG別結果と配信SHAを台帳へ記録する。

## 第3案：ユーザー最新指定（第2案と競合時はこちらを優先）

- DBG-027：エリアバナーは最終ステージの代表ボスをカウボーイショット（太もも付近まで）で配置。正式マスタのレベルとボス名を下部に常時表示。三河Lv.7直江兼続、尾張Lv.18徳川家康、美濃Lv.30織田信長。レベルを推測・固定せず正式ステージスナップショットから読む。
- DBG-014/030：編成先頭を飾る案を撤回し、保存済み戦闘ログに基づくMVPを表示。GAME03由来のBattleResultSummary.tsxとbattleResultScoring.tsをローカルGAME04内で確認した。GAME03リポジトリ・本番への書込みなし。
- MVP評価案：与ダメージ40、撃破20、回復20、シールド15、生存5。前4項目は味方内の最大値に対する相対評価、四捨五入。項目最大値が0の場合は0点。同点は与ダメージ、撃破、characterId順で決定する既存方式。表示専用の評価で戦闘・報酬計算には使わない。
- 合計スコアは既存形式の420msカウントアップ、内訳は520ms後から各90msずらして180msで表示。reduced-motionでは確定値へ。見本は合計の動作を実装し内訳は固定表示。本体では既存内訳の時間差演出も適用する。
- 勝利・MVP・与ダメージ/撃破/回復の要約を表示。その下に戦績/MVP詳細と報酬/EXPをそれぞれ折りたたみ、タップで展開。報酬を別ページへ遷移させる第2案を上書き。最下部は固定の次ステージ/一覧CTA。最終ステージ時は一覧を主CTAへ。実報酬の付与は既存settlementの一回分を表示するだけで、開閉により再付与しない。
- 報酬の初回バッジ・EXP共通パネルを折りたたみ内へそのまま移す。GAME03固有のRATE/RANK/BP/レイドチケットはGAME04のクエストへ持ち込まない。台詞は既存承認済みの戦国武将台詞が接続できる場合のみ使用し、GAME03台詞を流用しない。

### ログ接続の限定検証項目

GAME04のBattleResult.analysisにはdamage/healingはあるがkills/shieldはなく、GAME03のイベント列と同型ではない。承認後は保存frames/status.sourceId等を表示用adapterで集計し、ゲームを再シミュレーションしない。複数派の敵ID、複数hit、DOT/HOT帰属、シールド付与と消費、蘇生後の最終生存を確認する。欠落した帰属を勝手にactorへ付け替えない。旧ログで確定できないときはMVP集計未対応を表示し、推測したMVPを出さない。

見本65点は2敵へのDAMAGE/DEFEATと生存の説明用イベントを既存analyzeBattleResultへ入力して検証（40+20+0+0+5）。実戦ログの集計完了を意味しない。scripts/verify_b03_result_review.cjsとresult-v3-checks.jsonに記録。375/390pxでタップ開閉とCTA表示を確認。

## 第4案：DBG-014・027・030の最新構図と提示

- MVPリザルトの外周カード枠を撤去。勝利→左の腰上武将/右のMVP・名前・大きなスコア→小さい3列戦果→戦績詳細・報酬EXP→CTA。人物ブロックに四角い半透明帯は置かず、画面背景の右側の暗い放射状グラデーションと下部の緩いグラデーションで文字を保護。顔と名前欄を分離。ステージ背景は全面。
- スコアは最初に画面へ表示されたとき一回だけ420msで自動加算。再生ボタンは削除。スクロールで戻っても再カウントしない。動画は375/390幅で自動加算と詳細・報酬展開を記録。
- 戦績/MVP詳細と獲得報酬/プレイヤーEXPは同一リザルト内の独立したdetails。本文だけスクロール、CTAは別の固定行。主操作48pxの次のステージ、副操作44pxのステージ一覧は同幅。正式進行上次が存在/開放されない場合、一覧を主操作へ切り替える。
- 報酬はアイコン・数量・対象のみ初回バッジ、EXPは共通GrowthSummaryの方針を維持。見本の正式値ラベルは未接続であることを示す。集計済みの実戦値ではない。
- バナー164pxを維持。右に素材ごとに調整した頭〜太もも寄りの切り抜き、左にエリア名/進行状態、下端29pxの共通行にBOSS・名前・Lv。「最終ボス」の浮遊ラベルは削除。左側だけ局所グラデーション。外側の夜桜城門素材をやめ、承認済みquest-mikawa背景を出陣一覧背景として提案。全背景を暗くしない。
- scripts/serve_b03_review.cjs の /review-data.json はリクエスト時にquest65.jsonを読み、areaIdごとの正式並び順の最終ステージ→最終派→既存代表ボス規則で抽出。ステージ番号・件数・Lv・名前の固定なし。人物クロップのみcharacterId別の見た目設定で、数値/ボス選定を固定しない。新しいボス素材は統合時に切り抜きを限定確認する。
- 現在のチェックアウトは三河3/尾張4ステージ。並行改修の各5ステージ版はまだ未統合。5ステージ化を推測で作らず、最新マスタ統合後に最終ボスを再抽出する。見本の未挑戦は進行表示例であり、ユーザーの実保存状態を表さない。

### MVP採点・実ログ対応の区別

既存GAME03由来式：各項目round(本人値÷味方内最大値×配点)、最大値0なら0点。配点40/20/20/15/5、生存は最終状態で5または0。総点降順→与ダメージ→撃破→characterId順で同点を決める。GAME04で正式に記録されたdamage/healingと保存framesから確定できる撃破/シールド付与/最終HPをadapterに対応付ける。過剰ダメージ/過剰回復・DOT/HOT・蘇生・複数派・付与と消費の区別は本体実装時に限定検証する。帰属不明を推測してMVPを作らない。旧ログ欠損時は集計未対応を明示する。GAME03のRATE/BP等は移植しない。

第4案の65点・4200ダメージ・2撃破は前案で既存採点関数に通した説明用ログ値。今回も実戦ログ接続済みとは扱わない。実戦adapter、本体組込み、PRpush、新規配信は未実施。

提示ファイル：result-v4-{375,390}.png、result-v4-{375,390}-score.png、result-v4-{375,390}-rewards.png、area-v4-{375,390}.png、result-v4-{375,390}.webm。検証結果review-v4-checks.json。

## 第5回追加修正：承認済みレイド規則の再利用・記録MVPの実装

エリア一覧はRaidApprovedVisual.cssのraid-approved-card見出し19px・明朝系、badgesの12px/金枠、attributeの13pxを再利用。area専用差分は164px高・右側人物・左側copy・進行12pxのみ。独立BOSS下帯を除去し、名前とLvを同じ行に配置。375/390で実レイドと実エリアのcomputed font-family/font-sizeが一致。比較画像は同じアプリ実装のローカルQAから取得（一覧内容はオフライン検証データ）。正式QUEST_AREASの最終ステージからボス/レベルを選出、件数もstages.length。並行マスタ統合後に再確認。

RecordedBattleResultを本体BattleViewの完了表示へ接続。QuestViewはsettlementの既存報酬・playerGrowthを折りたたみへ渡す。仮の正式値・固定65点・集約予定テキスト・再生ボタンは本体に存在しない。報酬展開は表示だけで再付与しない。次ステージは正式解放判定で表示し、遭遇戦がある場合は既存の遭遇確認導線を保持。

採点の実装：recordedBattleMvp(result)。与ダメージと回復は保存済みanalysis.damage/healingを正本にし、記録エンジン同様、ダメージは直接攻撃/反撃（過剰分を含む）、回復は即時回復/蘇生を含む。DOT/HOTの量は誰かへ勝手に再配賦しない。撃破は各派のHPが正から0になった確定記録から集計。DOT撃破は付与者を一意に決められる記録のみ帰属、複数候補時はMVP集計不可を表示。シールドは新規付与の実量だけを加算し、吸収/残量減少を二重計上しない。生存は最後のHPで判定し蘇生後の生存も反映。

配点：与ダメージ40/撃破20/回復20/シールド15/生存5。前4項目はround(本人値÷味方内最大値×配点)、最大値0なら0点。生存5または0、合計最大100。同点は与ダメージ降順→撃破数降順→武将ID昇順。計算に必要なevent/analysisが欠落した旧記録、撃破帰属不明、シールド量欠落は推測MVPを出さない。戦闘ルール・保存済みsnapshot・報酬量は変更なし。

確認：型検査・モック設定build成功。正式1-1マスタをローカルエンジンで記録化した86フレームでMVPくノ一、damage876、kills2、heal0、shield0、生存5、合計65。これは前の説明用4200ダメージ/65点を接続したものではなく、同じBattleResult形式の計算結果。ユーザー保存戦闘を取得した検証ではない。候補の貢献を変えると先頭以外がMVPになること、シールド非二重計上、最終生存、曖昧DOT抑止、旧記録fallback、記録非変更を検証。

詳細パネルは不透明#19141c・本文#f5eee1、summaryは#211923f5、ヘッダーとフッターは本文から分離。375/390×664で二つのdetailsを開き、scrollHeight末尾まで到達・本文下端545px/CTA上端545pxを確認。0から中間値を経て65までの自動カウントをMutationObserverで取得。v5/interaction-checks.jsonに保存。実機は共通Preview配信後の確認。

共通ルールの更新先は `docs/product/GAME04_COMMON_UI_AUTHORITY.md` §18へ一本化した（2026-09-26）。本ファイルはB03案の来歴・見本を保持する資料とし、共通の値・部品契約を重複更新しない。今回の実装と375/390px見本は `common-ui/README.md`。
