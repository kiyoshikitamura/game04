# 統合と残件

## 現在の統合差分

1. `src/domain/redesign/data/quest65.json`: ユーザー採用43面273項目、配布A44箇所、独立比較条件を満たした追加12面125項目。バージョン `game04-quest68-development-balance-20260927`。
2. `src/domain/redesign/questMaster.ts` と API `source.ts`: 開始済み旧バージョンの戦闘を保存snapshotの報酬・進行仕様で精算する互換判定。旧snapshotへ新報酬を遡及付与しない。
3. API `index.ts`: 専用ブランチ内のローカル生成候補。実APIには配信していない。依存ソースのハッシュは `api-bundle-manifest.json`。
4. `/qa/stage68-local`: 既存BattleViewによる開発限定の計測ページ。共通BattleViewの改変なし。

`scripts/stage68_apply_adopted.mjs` は元の基準マスターに43面/Aを適用するためのスクリプト。**追加12面適用後に再実行して基準案だけへ戻さない**。最終データは検証済みのコミットから統合する。

## 並走修正を保持する統合順

2026-09-27の最終確認時、旧監査は `645512d94b918ecbe0a205085696baecaa6353f0`、共通Previewブランチは `067fe54de6aa6e2842b5d12a8b6e763c82341a08`。専用ブランチは別のまま作業した。共通側にはBattleViewを含むUI・inventory・tutorial等の後続修正がある。

- デバッグ統合担当が最新共通ブランチを基に、上記のソース差分を通常のGit差分として統合する。共通側のファイルを旧版で全置換しない。
- 共有ブランチを取り込んだ状態でAPIバンドルを**再生成**する。当ブランチのminify済み `index.ts` を共有版へそのまま全置換すると、並走ソースを巻き戻すおそれがある。
- 最終結合後に実装チェック、アプリ型検査、APIバンドル一致、保存済み戦闘のresume/settleを確認。未開始の新戦闘だけ新マスターを使うことを実接続環境で確認する。
- 共有Preview/API/DBへの反映、DB既存利用者の報酬補填方針、実機受入は統合担当の範囲。今回DB migrationや遡及配布は作成していない。新規初回クリア報酬のため、既に該当面をクリア済みの利用者への自動補填はない。開発検証の到達前資産は新規進行を前提とする。
- 共通UIの最終版で再生時間を再測定し、実機監査・G5を別に判定する。計算上の比較条件充足を受入合格へ置き換えない。

## 実行済みチェック

- `verify_stage68_implementation.mjs`: PASS。835入力で戦闘部分・到達前資産が一致。全68面の初回報酬差分と周回報酬保持、44箇所の先行配布、キャラ/装備育成の同一性、旧証拠・共通式の保持を確認。
- アプリ対象TypeScript: PASS。`scripts/stage68-app-tsconfig.json`（本体tsconfig継承、srcとnext-envだけ対象）で実施。
- リポジトリ全体 `tsc --noEmit` は保存済みの監査資料 `docs/verification/stage68-audit-20260926/evidence/deployed-api-v8.ts` の型エラーで失敗。既存証拠を編集して隠していない。全体型検査PASSとはしない。
- APIバンドル生成・sourceハッシュ照合: PASS。実APIへの接続検証・配信は未実施。
- ローカルBattleViewは実ブラウザ再生・画面確認済み。実時間はPLAYBACK.md。エミュレートされたローカル画面を実機監査と扱わない。

## 残件の区別と再開

|対象|区分|未達と次の処置|
|---|---|---|
|1-1|設計未達|到達前資産・同育成の候補が全勝に集中。初戦だけの複数戦法免除は採用済みだが、5帯免除ではない。保存済みのHP/ATK強化でも不足した。失敗を体験させる敵行動/対象設計を新たに決めるか、初戦の5帯条件を改めて判断する必要がある。無断で免除を拡張しない。|
|5-4|候補・証拠不足|不足は観測0勝。120候補探索でも最小2/200などの低勝率が残った。seedを都合よく選び直して0勝を作らず、到達前の不利属性/支援優先という別入力を追加する。理論的クリア不能を主張しない。|
|元の停滞14面中11面|設計未達|3-2、3-5、6-2、9-1、9-3、9-4、9-10、10-1、10-2、10-5、10-7。代表4編成だけ改善した案を面全体の解消にしない。|
|追加探索で停滞確認17面|設計未達|2-3、3-1、3-3、5-3、6-5、7-2、7-7、8-2、8-3、8-8、9-5、9-8、9-9、10-3、10-4、10-9、10-10。5帯が揃っていても長時間停滞の条件を満たさない。|
|調整後の実時間|証拠不足|9-6の代表・低勝率・旧停滞を測定。残りはnull。共通UI統合後、同じ条件で主/別/低率/停滞例を実再生して記録する。|

28面の失敗seedを1件ずつ診断再生し、`stall-diagnostics/<面>.json.gz` に最後の生存者、終盤50行動のダメージ・回復イベントを保存。原因は観測からの仮説であり因果を断定しない。`stall-next-candidates.json` に敵ID、変更前後、限定再検証条件を具体化した。

この敵個別案も `local-followup/<面>.json.gz` で限定試行した。主/別/5帯/最悪停滞編成の24試行で停滞がなく主全勝・別50%以上の候補だけ独立200試行へ進める。停滞が消えても勝率が全勝へ集中する候補は実装しない。これらは現行マスターとは別入力であり、現行判定へ混ぜない。

Node 24を使い、リポジトリルートで以下を実行する。`--import` は既存TSの拡張子省略import/JSONを読み取るローカルloader。

```sh
node --import ./scripts/stage68-loader.mjs scripts/verify_stage68_implementation.mjs
node --import ./scripts/stage68-loader.mjs scripts/stage68_implementation_local_followup.mjs 3-2
node --import ./scripts/stage68-loader.mjs scripts/stage68_implementation_counts.mjs
node scripts/build_stage68_api_candidate.mjs
node scripts/verify_game04_redesign_api_bundle.mjs
```

各実験は面別・候補別にatomic保存し、`complete` の面はスキップする。同じ入力を再実行する必要はない。次の敵値を試す場合は既存の完成checkpointを書き換えず、新しい実験ディレクトリと未使用seed範囲で、変更入力のみ比較する。採用する追加差分が出たら5帯、別攻略の中核実発動、技能個別非依存、到達前資産、全保存検証編成の停滞を再集約する。

追加2,711成功周回を必要とする配布削減案は非推奨の旧記録のまま。追加検証していない。共通300行動上限・式・バーストを変える必要性は立証しておらず、今回変更対象にしていない。
