# 実機デバッグ担当・統合担当への引継ぎ

専用ブランチ: `work/game04-stage68-audit-20260926`。基点はPR #37の d804d2a。監査の保存SHAはこの変更を含むコミットを使用する。監査表・ガイド・証拠・scriptsと既存DBG-047への追記だけを統合する。

1. common Preview側の最新HEADを確認する。数値/戦闘計算が変わっていなければ本監査コミットをcherry-pickする。台帳競合では本変更の「DBG-047 全68面再監査担当からの返却」節だけを最新台帳へ追記し、他番号の受付/配信記録を保持する。他番号は採番し直さない。
2. `src/`、正式JSON、API、DB、画像、UI、サウンドの変更は本コミットにない。コードビルド/新API配信はこの監査取り込みだけでは不要。画像・模擬戦・UI・演出・サウンドの修正配信を監査または調整承認待ちにしない。
3. DBG-047は「再現・ガイド条件修正完了／バランス案承認待ち」とする。「本番修正済み」「ユーザーの保存戦闘が勝利へ変更済み」にしない。保存戦闘は旧入力を保持するため、承認後も実機確認は検証アカウントの新規戦闘で行う。
4. 3-1の正式数値変更は `ISSUES_AND_PROPOSALS.md` の2箇所DEF1140→620案。現時点で採用/配信しない。承認されれば当該2フィールドのみ別コミットとし、現行/変更案の同条件200seed、対策除外200seed、前後面を検証。報酬・BURSTを戻さない。
5. 全68面の固定入力を再生するときはREADMEのreplayコマンドを用いる。監査中の探索スクリプトを主条件上書き用途に使わない。
6. DBの旧65面保存行とGitの保存index.tsを次回配信の正本にしない。実配信v8バイト・現行source/JSON・今回の適用経路を照合し、次の配信時にbundle生成/記録を一致させる。共有DBの保存行の役割は統合担当が明示する。
7. 7-8/8-3/10-10の全滅例、終盤のガチャ/覚醒/装備負担は残課題。10/68面だけが確定配布と検証済み育成で成立。残り58面は詳細条件を満たした場合の検証済み戦闘結果であり、入手経済の全面合格ではない。

既存承認正本は履歴として保持。エリア1/2各5面、1-1/1-3加入、1-2/1-4技配布、エリア任務無償輝石300を維持。main・本番・GAME03は変更しない。
