# R5 限定独立検証

2026-09-25。基準記録 180b45ab、実装 0e7f37c9、開発 API v24 の後続成果を保持。検証担当はゲーム実装・API・DBを書き換えない。

## 範囲と採用根拠

今回のユーザー指示を最新承認とする。AGENTS、MASTER_AUTHORITY_LATEST、侵攻正本、decisions の名称・画像対応表、日次・超過ダメージ比較資料を照合。過去資料の未承認記載は今回採用範囲で上書きする。

- SKD001〜024 は承認済み短名、025〜072 は設計名。72 ID、数値・効果・旧所持は保持。
- 既存40点＋候補32点、育成・交換10点を採用。背景は今回添付10＋15点を優先。
- SSR背景10点の素材採用と、未承認の解放・選択細則を分離。
- 日次 JST 0時失効・結果確定日計上・DM005 の侵攻令1枚が今回確定。DM004案は不採用。
- 新規侵攻のみ各攻撃時の実HP減少を算入。表示ダメージ、倍率、3勝条件、旧開催版を保持。

## 初期レビューの境界（実接続の最終結果は末尾）

|対象|確認する点|実施状況|
|---|---|---|
|名称・画像|承認表との全ID一致、配信先存在、数値/効果の変更なし、旧所持保持|コード・限定試験確認済み。実表示範囲は親のbrowser-limited.md、実API/DBは本書末尾を参照|
|背景|10エリア/5城3区分/10 SSR の一意対応、正規化画像・原本追跡、既存区分との一致|コード・限定試験確認済み。実表示範囲は親のbrowser-limited.md、実API/DBは本書末尾を参照|
|SSR機能|未承認細則を勝手に有効化していないこと|コード・限定試験確認済み。実表示範囲は親のbrowser-limited.md、実API/DBは本書末尾を参照|
|日次|JST境界・旧日受取拒否・DM005のみ1枚・再送非加算・G3抽選依存を分離|コード・限定試験確認済み。実表示範囲は親のbrowser-limited.md、実API/DBは本書末尾を参照|
|新侵攻|shield/多段/反撃/DOT/回復/蘇生、旧Lv精算、再送、旧開催互換|コード・限定試験確認済み。実表示範囲は親のbrowser-limited.md、実API/DBは本書末尾を参照|

実表示・認証API・DBは親の証拠と突合する。型検査やドメイン試験のみで実表示・実機を合格扱いにしない。全画面監査・全機能の再受入は行わない。

## 事前指摘

- 日次受取評価と master ID 検索が別々に Date.now() を読む既存経路は、1回の基準時刻へ固定することを親へ通知した。
- 侵攻の実HP集計は開始HP総和による一括上限では代替できない（回復・蘇生で再減少するHP、shield吸収の除外）。既存 D03 提案の採用根拠を親へ通知した。

## 判定

ローカル限定検証 PASS。実API・DB・Preview・実機の受入を本レビューのみで代替しない。G2全体の完了判定は行わない。

## 実施した独立検証

- `node scripts/verify_game04_g2_r5_rules.cjs`：JST境界失効、結果日所属、同一イベント再送、日次受取再送拒否、DM005のみ令1枚、基本8項目のみ達成数計算、3勝資格、新規版HP上限・旧Lv非転用・旧snapshot同値を確認。
- `scripts/verify_game04_g2_r5_damage_edges.cjs` を独立担当が追加・実行。多段／shield／DOT／反撃／回復／蘇生の6条件が実際に発火することを要求し、全フレームの敵HP減少を独立再集計。actualHpDamage と全件一致。マーカーを付けない旧入力の結果・全フレームと、actualHpDamage以外が完全一致。結果は `damage-edges-independent.json`。
- 回復例は開始HP200に対して累計11,807、蘇生例は開始HP合計370に対して4,920。開始HP総和で一括capしていないことを確認。多段表示は10,095、実HPは500。演出hitsを実HPへ二重算入しない。
- `node scripts/verify_game04_r5_approved_assets.cjs`：72名称・画像存在、792 LB行の数値・効果不変、素材10点を確認。別途承認対応表とmanifestを直接照合し、新規32点の元ファイル・既存40点のパス、配信画像SHA256・PILデコードを全72点で確認。
- `node scripts/verify_g2_r5_backgrounds.cjs`：35点SHA256/WebP、エリア10、5城60レベルの通常/関門/城主15パス、再生は保存開始Lvを使用、開始情報のない旧snapshotは旧背景fallbackを確認。
- SSR専用背景は配信素材と対応表だけを追加。HOME_BACKGROUNDS への追加・解放状態変更はなく、未承認細則は有効化していない。`ssr-contract.md` の SBG-01 に集約。

## 修正・未確認の区別

日次受取の時刻読取りは実装担当が単一 now に修正済み。親方針に沿い新規侵攻では倍率適用後も実際の共有HP減少量を貢献上限にする。旧開催は旧full damage集計を保持。コード上の阻害不具合は本確認範囲で検出なし。

認証APIでの実受取・新旧開催保存、配信HTTP、モバイル実表示の限定受入は親の結果を参照すること。取得していない証拠をここで合格へ昇格しない。

## v25 実API・DB独立受入

実装 `9e9a0a67da69ee34c9297b123bdf4bf7e06e233b`、開発 `lrgyllgzcdcphlbmkknc` API v25。既存専用QA A/Bを使用し、通常の認証API操作だけを実施。DBはSELECT照合のみ。追加fixture・SQL変更なし。

|対象|実接続結果|根拠|
|---|---|---|
|DM005達成|QA-A mikawa-1勝利で当日出陣2→3、DM005達成|battle `b935bc22-a050-44a2-b72a-8091f7bed823`|
|受取・再送|侵攻令2→3、同request再送200でstate不変、新IDの再受取400、前日ID400|claim `54ac31cd-5ad9-4ee4-b1a4-e8140e8a007f`、state version31|
|DB照合|令3・DM005受取済1件、成功receipt1件、拒否2IDは保存なし。銭差分+2,000|`daily-db.json`、`daily-receipt-db.json`|
|新侵攻開催|TI01通常API開催成功、令3→2、新policyをsnapshot保存|room `8ebe12e0-2637-412b-a6fa-ba6744551932`|
|新侵攻戦闘|表示38,756／敵実HP減少25,830／倍率1.5後38,745。共通HP78,000→39,255、貢献38,745|battle `54ab7396-de24-42e0-8e20-66b9f19d0305`、state35／room2|
|新侵攻再送・再読込|同battle ID再送でstate・room完全一致、get_stateも一致。DBも同値|`live-api-independent.json`、`raid-db.json`|
|旧侵攻互換|旧snapshotで1戦。新policyなし、actualHpDamageなし、旧表示ダメージ1,445をそのまま算入。HP61,492→60,047、貢献差分1,445|旧room `75b670f1-ca4f-482f-b8ea-8f0a5a92cb3f`、battle `09ff2bf4-afb8-4a32-a237-4d07cf4eb48f`|
|正式画像接続|新侵攻保存結果に SKD033「気合」既存画像、SKD008「水断」新画像を確認|`live-api-independent.json` enemySkills|

限定API計12操作のrequest ID／HTTP／所要時間／状態版を `live-api-independent.json` へ保存。生レスポンス・session秘密は記録へ含めない。初回試行で action 名を誤って `mission_claim` とした要求は400で未変更、正規 `claim_mission` に訂正して検証した（実装不具合ではない）。

実際のJST0時を跨ぐ待機は行っていない。日付境界は注入時刻のローカル試験、当日受取／前日拒否は実APIと区別。今回の新侵攻1勝は段階を撃破していないため、共有HP端数cap・3勝・段階跨ぎの境界根拠はローカル限定試験を維持。実機・自然進行・G3抽選入手を合格にしない。
