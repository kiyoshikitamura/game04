# R5 正式スキル・素材採用

2026-09-25 追加指示を採用根拠とする。承認された対応表の72名称（001〜024短名・025〜072設計名）、既存40行＋新規32行の画像、育成・交換素材10点を接続。レアリティは N/R/SR/SSR の文字表示を維持する。

## 接続

- `data/formal-skill-presentation.json` を単一の正式表示表とし、`getFormalRaidSkill` 経由で所持一覧・詳細・編成・準備・戦闘入力へ反映。数値正本の履歴列 `name_provisional` は性能正本の構造保持のため残すが、本体表示では使用しない。
- 既存40行は承認済み個別pathを維持。新規32点は `public/skills/formal/SKDxxx.webp`（512px、WebP q88）。候補原本は変更しない。
- 素材10点は `public/items/formal/`（256px、WebP q88）。SR/SSR汎用魂は候補資料の最新WebP（v2反映済み）を使用。武将／装備XL、汎用魂4、選択券4を育成・魂交換・在庫表示へ接続。
- 報酬画面向け `growthRewardImage` を共通化。`CanonicalItemIcon` も同じ10素材IDを参照し、商店・BOX・受取表示の◆代替を解消。正式ID、在庫キー、効果、数量、EXP、旧所持資産および既存戦闘snapshotは変更しない。
- 新規戦闘入力は新名称・新画像を含む。旧snapshotを再計算・表示情報一括書換えする変更は行わない。

## 限定検証

**モジュール検証 PASS**。`node scripts/verify_game04_r5_approved_assets.cjs`：正式72件×LB0〜10の792件を採用前実装と比較し、名称・画像以外が完全一致することを確認。承認表との名称一致、画像ファイルの存在、素材10IDの接続、未知IDが別素材へ誤変換されないことを確認。

配信後HTTPと本体の限定表示確認は親の統合記録へ追記する。上記モジュール検証を実機受入とは扱わない。

## 対応表

`approved-assets-manifest.json` に72スキル＋10素材の参照元、配信path、SHA-256、寸法、サイズ、素材在庫キーを保存。`newDeliveryFile:false` の既存画像はローカル不在分を既存取得キャッシュで復元しただけで、Git更新対象にしない。

## 判断待ちの解消

正式スキル名・72画像対応・素材10画像・レアリティバッジ制作は今回の指示で確定済み。旧 decisions 文書の未承認記載は当時の履歴であり、現在の判断待ちではない。

React限定レビュー：追加表示は同期の純粋path resolverのみ。新規Effect・リクエスト・保存操作なし。装飾画像は隣接の正式名称・レア文字を保持してalt空、素材アイコン寸法とcontain指定を固定。既存fieldset busy無効化・数量入力・保存条件を保持。TSX2ファイルの構文変換エラーなし。
