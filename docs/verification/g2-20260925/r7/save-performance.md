# G2 保存性能 最終整理（API v31）

判定: **保存は未達。1.5秒例外も未成立。** v31は結果後の「閉じる」操作可能まで1秒以内0/20、1.5秒以内18/20。必要な19/20に届かないため、中央値1.30秒を根拠に合格へ変更しない。以後のAPI追加改善は停止し、残件として提出する。

## 条件・版対応

- 対象: 武将のおまかせ編成/装備保存に対応する `save_deck`。同一QA-C、cloud Chrome、390×568、既存iframe/tab 1を保持。frontendは `af640e291d3e79e511833dcef1e16f0ff7528b94` の読み込み済み画面を継続使用。
- v28→v29: 認証後のprofile/prior/acquisition読み取りを並列化。状態初期化・commit・再送順序を保持。
- v30: G3後続（正式券user_items接続等）を保持した追加比較の基準。v29→v30はG3変更も含むため保存改善単独の比較ではない。
- v30→v31: 同frontend・同タブ・同QA・同操作。rooms/owner投影を1RPCに集約し、依存するHTTP往復を削減。v30のG3成果と並列化を保持。
- 最終実稼働API: **v31**、管理API hash **`70c20710ef9a7a36b103630cd67548da2fa62f314efa2d0797a482db11fb9c6f`**。親が実反映・本文一致を確認。保存候補ファイルSHA256 `59e497ae9278ba1108ed3a31f510bd4d266eac6535c815bc5f26692f29eac215` は別のhash種別として区別する。
- 共有開発DB `lrgyllgzcdcphlbmkknc` でG3/P31作業等が並走し、負荷・接続条件の完全隔離はしていない。短縮の全量をコード差分だけの効果と断定しない。
- iPhone/Safari実機、ユーザーのWi-Fi/4G/5Gは未測定。本結果は代替環境の測定であり実機合格ではない。

## 結果・次操作可能まで（主判定、ms）

pointerdownから保存成功後の「閉じる」CTA readyまで。busy表示だけを完了にしていない。各20回、全試料を保持。p95は昇順のceil(0.95×n)番目。

|API版|n|中央値|p95|最大|≤1000ms|≤1500ms|判定|
|---|---:|---:|---:|---:|---:|---:|---|
|v28|20|1716.40|3086.10|3397.70|0/20|0/20|未達|
|v29|20|1562.20|2037.90|2102.10|0/20|9/20|未達|
|v30|20|1408.85|1674.10|1684.30|0/20|12/20|未達|
|v31|20|1297.70|1928.90|1954.50|0/20|18/20|未達、例外未成立|

v30→v31の中央値は111.15ms短縮、1.5秒以内は12→18回。ただしp95/最大は悪化し、1.5秒超が2回（1928.9/1954.5ms）。全回3秒以内でも19回条件を満たさないため例外合格にできない。

## API待ちと結果DOMの分離（ms）

同一timeOrigin `1790314682705` のCTA開始から150ms以内に開始する同名request/action metricを一意対応し、各版20/20を抽出した。集計は `save-api-matched-summary.json`。`request / save_deck` はクライアントからのAPI待ちであり、認証・内部DB・通信をさらに分解したサーバー計測ではない。

|API版|API中央値|API p95|API最大|API ≤1000/≤1500|結果DOM中央値|結果DOM p95|結果DOM最大|
|---|---:|---:|---:|---:|---:|---:|---:|
|v28|1683.80|3042.70|3363.80|0/20・0/20|1686.35|3045.30|3367.50|
|v29|1514.85|1990.30|2042.40|0/20・9/20|1517.05|1992.50|2046.10|
|v30|1362.25|1622.20|1649.40|0/20・12/20|1364.90|1625.50|1651.50|
|v31|1249.80|1887.80|1907.90|0/20・18/20|1252.25|1889.90|1909.90|

結果DOMはaction開始→成功応答state/busy解除commit。CTA readyとは起点と観測タイミングが異なるため、両中央値の単純差を厳密な描画時間としない。保存待ちの主要部分がAPIであることと整合する。

|API版|処理中DOM n|中央値|p95|最大|
|---|---:|---:|---:|---:|
|v28|20|2.80|4.30|7.70|
|v29|20|3.35|4.90|7.30|
|v30|20|3.05|4.20|5.10|
|v31|20|3.10|5.50|5.70|

処理中DOM commitは全回100ms以内。ただし実paintの計測ではなく、タップ反応0.1秒基準の全受入を代替しない。

## 画像と未測定範囲

今回r7のrawはCTAとaction/request metrics。画像resource/decode完了の独立ログは含まれないため、v28〜31の画像待ちを0msとは記載しない。画面と素材を保持したwarm保存比較である。旧r6では保存中の新規画像グループなしとの記録があるが、v31の画像計測済み証拠へ流用しない。

この比較はsave_deckのみ。受取・交換それぞれの20回、cold独立5回、実機/Safari/ネットワーク別、実paintは本書の受入対象外・未測定。実戦闘開始20回は別担当の証拠に分離する。API版・frontend版が異なる旧r5/r6の合格/不合格も、この版の実施証拠に読み替えない。

## 証拠

- `browser-v28-before.json` / `browser-v29-after.json`、CTA集計 `save-comparison.json`
- `browser-v30-before.json` / `browser-v31-after.json`、CTA集計 `rooms-save-comparison.json`
- `save-api-matched-summary.json`: 上記rawの同時刻metricを重複排除・対応付けて集計。

v28/v29 rawの文字列表現には末尾省略があるため、そのファイル単体で全metricsが揃うとはしない。v30/v31 rawに保持された履歴と併用し、CTAごとのAPI/feedback/resultを各20件一意に対応できた範囲だけを集計した。省略を0値で補完していない。
