# R01・R04・R13 限定実接続検証

対象: GAME04 dev `lrgyllgzcdcphlbmkknc` / API `game04-redesign-api` v23（開始時）。親が作成・QA分類した専用アカウントだけを操作。全画面監査・正式初期付与・自然進行の検証ではない。

## 方法と証拠

- R01: 正式マスター不明の所持スキル・装備を各1件保持。実装の `autoEquipSkills` / `autoEquipEquipment` で編成を計算し、認証HTTP `save_deck` → `get_state`。選択除外、元所持保持、装備の重複なしを照合。ブラウザボタン操作とは分離。
- R04: 親の限定fixtureで先頭武将の固有魂34。10→5 / 12→6の交換、同requestId再送、最小数未満・奇数・不足の拒否を認証HTTP→再読込で確認する。API初回応答後の試験スクリプトに初期 `growthInventory` 未作成の想定漏れがあり停止したが、ゲーム側は正常成功。既存requestIdを再送し、重ねて新規消費せず続行する。
- 通信境界: 送信前中断なら非消費。成功応答をクライアントが破棄した場合は、再読込で1回分の消費を表示し、同requestId再送で重複消費しないことを区別する。これは実際のブラウザ通信障害操作の証拠ではない。
- R13: Player Lv1/累計EXP99から正式8-1（`izumo-1`、EXP144）勝利でLv3/累計243。行動力149保持と行動力20→100の2条件。武将Lvアップとは別。ステージ解放・強化5人は専用QA fixtureでありG4自然進行受入ではない。

準備資料: `r01-r04-fixture-template.sql`（適用は親記録が正本）、`r13-party-fixture.json`。R13の敵・ステージEXP・報酬マスターは変更しない。強化編成はseed1/42/20260925で4Wave勝利を事前確認したが、これは実API受入を代替しない。

実行スクリプト: `scripts/verify_g2_r4_inventory_live.cjs`、`scripts/verify_g2_r4_player_recovery_live.cjs`。秘密sessionファイルはGitへ保存しない。

## 発見・修正した不具合 R01/R04/R08

装備所持時、`captureMissionAssets` が任務履歴の装備オブジェクトを毎回 `{masterId,level,lb}` 順に作り直す。PostgreSQL jsonb再読込は `{lb,level,masterId}` 順になるため、API `stateFor` のJSON文字列比較が値不変でも差分と判定し、`get_state` ごとに無用な保存とversion増加が発生した。実APIでv4→8→12→17と観測し、同一交換requestIdの再送でも資源は重複消費しないが返却versionが上がった。

修正: `missionProgress.ts` の既存履歴オブジェクトのキー順を保持して最大値だけ更新。未知所持・既存任務履歴を削除しない。`verify_g2_r4_mission_read_stability.cjs` でjsonb順序への往復、値不変で保存差分なし、実成長は更新、履歴最大値保持をPASS。独立担当が元状態非破壊・履歴最大値保持・実増分・反復JSON安定を確認。親がAPI v24へ配信し、実get_state再検証を開始。

修正前HTTP証拠: `r01-r04-before-read-stability-fix.json`。最初の試験スクリプト停止と、本不具合でのversion検査失敗を区別する。

## 状態

R04 認証HTTP・DB・再読込・同requestId再送はPASS（API v24）。`r01-r04-live.json` / `r04-db-evidence.json`。

- 初回34→24/汎用N0→5、2刻み12→6で24→12/5→11、成功応答破棄試験10→5で12→2/11→16。
- 8個・11個・在庫不足は400、在庫・版不変。送信前中断も非消費。
- 応答破棄後に読込と同requestId再送して、一度分だけ消費。通信障害時に「必ず非消費」と誤表示しない契約を確認。
- 修正後 `get_state` と既存交換再送がv17で安定、12個交換のみv18、次の10個交換のみv19。DBも固有魂2/汎用N16/v19一致。

R01は未知装備のみ→空欄保存と、有効装備30＋未知1の混在状態のおまかせ保存がPASS。DBに所持31保持、編成装備30/重複なし/未知非選択を確認。正式スキルと旧所持を保持。`r01-mixed-inventory-live.json` / `r01-mixed-db-evidence.json`。実装関数で計算した編成を認証HTTP保存した証拠であり、ブラウザボタン操作の主張ではない。

R13超過保持ケースPASS: 正式8-1勝利、Player Lv1/exp99→Lv3/exp243、energy149維持/回復0、state v25→再読込・同ID再送v25不変。DB users Lv3/xp22/vitality149とstate累計exp243一致。`r13-overcap-live.json` / `r13-overcap-db-evidence.json` / `r13-overcap-wallet-evidence.json`。不足→100ケースもPASS。Lv1/exp99/energy20→Lv3/exp243/energy100、回復80のみ。2Lv分を積み増ししない。state v26→28、再読込・同ID再送v28不変。DB users Lv3/xp22/vitality100とreceipt回復80が一致。`r13-refill-live.json` / `r13-refill-db-evidence.json`。

## 親への統合結果

- R01: API保存・再読込・DB保持は完了。共通原因の無用再保存不具合を修正しv24実APIで安定確認。
- R04: 交換成功、入力拒否非消費、送信前中断非消費、応答破棄後の再読込と同ID再送非重複は完了。ブラウザの送信中UI実操作はこのHTTP試験では未確認（既存局所試験継承、実機個別確認方針）。
- R13: Player複数Lvアップ時の超過保持・一度だけ上限回復・再送非重複を実API/DB/receipt/再読込で完了。
- G2全体判定は行わない。専用Previewの変更SHA/API v24対応は親の統合記録へ接続する。UIの送信中固定・二重操作防止は既存局所試験を継承し、実機合格とはしない。
