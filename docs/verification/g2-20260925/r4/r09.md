# R09 計測接続・限定検証

2026-09-25。PR #30 記録20c5cde5のR09を継承。共有API/DB適用・Preview配信は親が管理。

## 実装済み（統合受入とは別）

- `gameplayMeasurement.ts`: `character_awaken`のallowlist抜けを是正。`raid_claim`は部屋の受取前後を比較し、自分の未受取→受取済みgrantのみ記録。grant ID・報酬kind/ID/数量を状態/room保存と同transactionへ渡す。別requestによる既受取確認は新受取receiptを作らない。過去receiptの推測補完なし。
- `source.ts`: 上記receiptと、既存認証に従う `observe_state_restore` を接続。ユーザーIDは認証結果を使いpayloadから受け取らない。
- `RedesignApp.tsx` / `redesignRestoreObservation.ts`: 本体のReact stateに初回復帰データが採用された後、ownerごとに一度だけバージョンACK。定期refreshでの水増しなし。失敗は未計測であり、ゲームstateやローディングを失敗させず再試行ループなし。これは外部認証・別端末復帰の合格証拠ではない。
- SQL候補 `supabase/manual/game04_g2_kpi_observations.sql`: RLS・service_role専用ACKテーブル/RPCとreceipt詳細集計RPC。ACKはDB版と一致したものだけ記録、request重複を排除しゲームstateを変更しない。
- 詳細RPC: 新規claim grantの報酬数量、ACK日次、既存acquisition subjectと行動を結合。subjectごとの最初の結合済みjourneyに限定し複数流入で行動が増えない。時点QA除外、qa_v1、unmapped/unboundを明示。件数を転換率と呼ばない。
- `/api/admin/kpi/v2/gameplay`: gameplay/supply/detailを同期間で並列取得。どれか未適用ならエラー、欠落を空実績として隠さない。
- `/admin/kpi/game04`: 専用の読み取り専用表示。既存Basic認証matcher内、GAME04開発URLに限定。QA除外区分、数量、ACK、流入連結を表示。grant件数は複数報酬行を合算不可、ログボbundleは個別item量ではないことを明記。

## ローカル検証

- `node scripts/verify_game04_g2_kpi.cjs`: PASS。新規grantだけ・複数報酬数量・他ユーザー/既受取除外・再送空receipt・元room非破壊・awakening記録。
- 同試験で既存Basic境界: 設定なし503、未認証/誤認証401、試験資格情報だけ継続、新ページmatcher対象。
- `npx tsc --noEmit --pretty false`: PASS。

## 親への適用・実検証

1. 稼働開発 `lrgyllgzcdcphlbmkknc` を確認してSQL候補のみ適用。過去migration再適用なし。
2. `supabase/tests/game04_g2_kpi_observations_rollback.sql` を実施。既知QA d6dabf02のみ、合成request/ACKは全rollback。既存QA分類をそのまま使用し、時点除外trueをassert（分類期間の重複追加なし）。grant数量123/2、別request重複grant、QA除外、ACK再送/古い版、state不変、subject結合、権限をassert。
3. 同一候補API/Previewで実 `raid_claim` のgrant受取→保存→再送→詳細数量を照合。本体初回ロードACKを実request/DB照合。
4. 配信HTTPの `/admin/kpi/game04`、`/api/admin/kpi/v2/gameplay?from=2026-09-25&to=2026-09-25` を既存管理者設定で確認。未設定なら503、勝手に設定を追加しない。正しい認証/誤認証/一般RPC禁止を分離。

SQL適用は親から成功連絡。最初のrollback試験は既存分類期間への重複fixture INSERTで失敗したため、分類追加を除去し既存QA時点除外assertへ修正。数量/件数比較はNULLを成功扱いにしないIS DISTINCT FROMへ強化。修正後rollback・実HTTP・実UI→receipt照合は親の実施記録を優先する。

親の実SQL検証で詳細RPCのPL/pgSQLローカル変数 `result` とrequests列 `result` の曖昧参照42702を検出。factsの全テーブル列をalias修飾し、ローカル変数を `aggregate_result` へ改名。保存SQL候補を是正し、詳細RPCのみの修正SQLを親へ渡した。APIは親からv24配信済み連絡。修正後のSQL受入は親の再実行結果を参照する。

## 親の開発環境受入結果（2026-09-25）

- DB migration `20260925003813 game04_g2_kpi_observations`、`20260925003959 game04_g2_kpi_detail_qualified_columns` の2件だけ適用。後者の実適用SQLを `r09-applied-detail-fix.sql` に保存。42702修正後、rollback SQLの全ASSERTが成功し合成行は残っていない。
- API v24で共闘1 grant・6報酬行を実受取。receipt・DB・再送と集計数量は一致（銭5000、魂1、大経験素材各1、スキル素材2、装備突破2）。各報酬行のgrant_count=1を足して6 grantと解釈しない。全行QA excluded。詳細は `r03-live-db-claim.json` / `r09-live-sql.json`。
- 同コードPreviewの既存専用QAを実ブラウザで復帰。React採用version17のACKを1件保存し、その後の10遷移・定期取得後も1件のまま。流入web_v1と結合するがQA excluded。外部ログイン受入とは別。
- 実DB権限: anonの詳細RPC実行=false、authenticatedのACK実行=false、service_roleの詳細RPC実行=true。
- 不変Preview `https://game04-nh6a0tnip-kiyoshi-kitamura.vercel.app` の管理者API実HTTPは **503 / KPI authentication is not configured**。既存設定を勝手に追加せず、Previewの `KPI_BASIC_AUTH_USER` / `KPI_BASIC_AUTH_PASSWORD` 設定および許可された資格情報が利用可能になった時点で認証HTTPの200/401・期間・表示を受け入れる。ローカル認証分岐PASSを実HTTP合格にはしない。
- 過去の分類不明データを一括QA指定・通常実績認定していない。既存included/unmappedも独立した根拠確認が必要で、今回の専用QA除外成立と区別する。

追加の直接REST権限検証は `r09-http-rpc-permissions.json` を参照。管理者HTTPとは別経路として、anonおよび専用QA authenticatedが詳細RPCへ直接アクセスできないことを確認。
旧分類の具体的な数と証拠範囲は [限定照合](r09-classification-scope.md) を参照。旧180は人数ではなくbattle行数。分類変更なし。
